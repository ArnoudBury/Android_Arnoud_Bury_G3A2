package com.example.countryapplication.model.index

import com.example.countryapplication.model.Flags
import com.example.countryapplication.model.Name

data class CountryIndex(
    val name: Name,
    val flags: Flags
)